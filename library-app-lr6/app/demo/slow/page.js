export default async function SlowPage(){await new Promise(r=>setTimeout(r,3000));const res=await fetch('https://jsonplaceholder.typicode.com/posts');const posts=await res.json();return <div className="container section"><h1>Повільна сторінка</h1><div className="grid">{posts.slice(0,6).map(p=><article className="card card-body" key={p.id}><h2>{p.title}</h2><p>{p.body}</p></article>)}</div></div>}
