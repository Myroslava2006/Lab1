export default async function NoCachePage(){const r=await fetch('https://jsonplaceholder.typicode.com/posts/3',{cache:'no-store'});const p=await r.json();return <div className="container section"><h1>Пост без кешу</h1><div className="error">Кожне оновлення — новий запит.</div><article className="card card-body"><h2>{p.title}</h2><p>{p.body}</p><p className="muted">Завантажено: {new Date().toLocaleTimeString()}</p></article></div>}
